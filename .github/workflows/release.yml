name: Release

on:
  push:
    branches: [main]

permissions:
  contents: write
  id-token: write
  pull-requests: write

jobs:
  release-pr:
    name: Create Release PR
    uses: rayxiom/workflows/.github/workflows/rust-release.yml@v1
    permissions:
      contents: write
      id-token: write
      pull-requests: write
    with:
      command: release-pr

  release:
    name: Release
    uses: rayxiom/workflows/.github/workflows/rust-release.yml@v1
    permissions:
      contents: write
      id-token: write
      pull-requests: write
    with:
      command: release
